[package]
name = "pucksdata"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.11", features = ["blocking", "json"] }
clap = { version = "4.4", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
# For future use with mocking HTTP requests
# mockito = "1.2"
# wiremock = "0.5"
